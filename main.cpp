#include <iostream>
#include <stdio.h>
// HERNANDO URIEL TORRES DE LUNA 203'342

struct nodo{
    // string con el nombre del vendedor
    // array dinamico con el nombre de los clientes
    struct nodo *sigiente;
};

typedef struct nodo *NODO;
typedef struct NODO *PNODO;

// crear funcion para eliminar y buscar

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
